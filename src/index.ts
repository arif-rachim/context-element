import {ArrayContextElement} from "./array-context-element";
import {ContextElement} from "./context-element";
import {ARRAY_CONTEXT_ELEMENT_TAG_NAME, CONTEXT_ELEMENT_TAG_NAME} from "./types";

customElements.define(ARRAY_CONTEXT_ELEMENT_TAG_NAME, ArrayContextElement);
customElements.define(CONTEXT_ELEMENT_TAG_NAME, ContextElement);
